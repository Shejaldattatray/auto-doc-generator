```markdown
### `connect()`

**Purpose:**
Simulates the successful establishment of a database connection. This function does not perform an actual connection but returns a string to indicate a connection status.

**Parameters:**
None

**Returns:**
*   `str`: A string indicating that the database is connected.
    *   Example: `"Database Connected"`
```